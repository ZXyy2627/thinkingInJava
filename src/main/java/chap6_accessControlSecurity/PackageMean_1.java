package chap6_accessControlSecurity;
//这里的package就代表了当前类所在的包

//如果要引入其他包的类，就使用import关键字
/**
 * 什么是包？
 * 包就是一组类的空间组织单元
 *
 * 如何防止类名之间的冲突，就应该使用包来隔离，
 * 例如：util包中可以有类JDBCConnection类，sql包中也可以有一个同样叫JDBCConnection的类
 *
 */
public class PackageMean_1 {
}
